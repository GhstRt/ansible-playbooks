---
- name: Update user password on multiple servers
  hosts: all  #
  become: yes 
  gather_facts: true
 
  tasks:
    - include_vars: vars4user
    - name: user password is updated
      user:
        name: "{{ user }}"
        password: "{{ new_password | password_hash('sha512') }}"
        update_password: always   
